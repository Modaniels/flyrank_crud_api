[project]
name = "flyrank-crud-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Modaniels", email = "modaniels507@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
]

[project.scripts]
flyrank-crud-api = "flyrank_crud_api:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
